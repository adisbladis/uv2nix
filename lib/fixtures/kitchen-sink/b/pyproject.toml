[project]
name = "b"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "certifi>=2024.7.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
